from app.admin import models

def create_user(user: models.User):
    # Implement user creation logic here, such as hashing the password and saving the user to the database

def get_user(user_id: int):
    # Implement user retrieval logic here, such as querying the database for the user with the specified ID
